import { applicationGeneric } from './application-generic';

describe('applicationGeneric', () => {
  it('should work', () => {
    expect(applicationGeneric()).toEqual('application-generic');
  });
});
